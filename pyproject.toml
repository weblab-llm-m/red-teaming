[project]
name = "red-teaming"
version = "0.1.0"
description = "Medical LLM red-teaming test framework for access control evaluation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.6.1",
    "python-dotenv>=1.2.1",
]
